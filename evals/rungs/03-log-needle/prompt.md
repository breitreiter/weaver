Some service is logging about connection-pool exhaustion. Which service is it,
and what is its connection pool limit (pool_max)?

Answer schema:

```json
{
  "service": "<service id>",
  "pool_max": 0
}
```
