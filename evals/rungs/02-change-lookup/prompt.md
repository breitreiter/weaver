What version of `payments-api` was deployed during the observed window, and at
what time?

Answer schema:

```json
{
  "version": "<version string>",
  "deployed_at": "<time of day (HH:MM:SS) or full ISO-8601 timestamp>"
}
```
