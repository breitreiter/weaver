Compared with the base window, which single **service** had its own error rate
move the hardest, and on which metric? Answer for an individual service — not a
service-to-service dependency edge (the "A→B" rows).

Answer schema:

```json
{
  "service": "<service id>",
  "metric": "<metric name>"
}
```
