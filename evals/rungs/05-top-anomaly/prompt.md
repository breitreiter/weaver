Compared with the base window, which single **service** had one of its own
signals move the hardest, and which metric was it? Answer for an individual
service — not a service-to-service dependency edge (the "A→B" rows).

Answer schema:

```json
{
  "service": "<service id>",
  "metric": "<metric name>"
}
```
