Order these four services by when their **error rate** first moved, earliest
first: web-gateway, payments-db, checkout-api, payments-api.

Answer schema:

```json
{
  "order": ["<service>", "<service>", "<service>", "<service>"]
}
```
